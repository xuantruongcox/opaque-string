# opaque-string

## Instructions for Opening quiz-collection.docx

1. Navigate to each following directories:
   ```
   ./Topic-1/
   ```

2. Locate the file named `quiz-collection.docx`

3. To open the file:
   - Double-click the file if Microsoft Word is your default .docx viewer
   - Or right-click the file and select "Open with" > "Microsoft Word"
   - If Microsoft Word is not installed, you can use other compatible applications like:
     - LibreOffice Writer
     - OpenOffice Writer
     - Google Docs (by uploading the file)

4. The document should now open in your chosen application

**Note**: Make sure you have a compatible document viewer installed on your system to open .docx files.